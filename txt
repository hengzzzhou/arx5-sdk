cd ~/code/robotic/arx5-sdk/build && cmake .. && make -j 
sudo ifconfig can0 down 2>/dev/null; sudo killall slcand 2>/dev/null; sudo slcand -o -f -s8 /dev/ttyACM0 can0 && sudo ifconfig can0 up  
  sudo slcand -o -c -s8 /dev/ttyACM1 can0      
  sudo slcand -o -c -s8 /dev/ttyACM2 can1                                                                        
                                                                                                                 
  # 启动接口
  sudo ip link set up can0
  sudo ip link set up can1